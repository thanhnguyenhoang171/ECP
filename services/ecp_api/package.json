{
  "name": "ecp_api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const cmd = require('os').platform() === 'win32' ? 'mvnw.cmd' : 'mvn'; require('child_process').execSync(`${cmd} clean package -DskipTests`, {stdio: 'inherit'});\"",
    "dev": "node -e \"const cmd = require('os').platform() === 'win32' ? 'mvnw.cmd' : 'mvn'; require('child_process').execSync(`${cmd} spring-boot:run`, {stdio: 'inherit'});\""
  }
}
